Change the directory to where the cpp file is.

In the terminal, compile my code by using command:
g++ -std=c++1 BST.cpp -o BST

Then it will compile to a BST file.

Then run the BST file by using this command:
BST inputfile

The inputfile means the path to the testfile.